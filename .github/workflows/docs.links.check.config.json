{
    "ignorePatterns": [
        {
            "pattern": "^(?!https?:\/\/)"
        },
        {
            "pattern": "^http://localhost"
        },
        {
            "pattern": "^http://yourip"
        },
        {
            "pattern": "^http://your-ip"
        }
    ]
}
